service: serverless-node

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs12.x

plugins:
  - serverless-offline
  - serverless-dotenv-plugin


functions:
  hello:
    handler: handler.hello
